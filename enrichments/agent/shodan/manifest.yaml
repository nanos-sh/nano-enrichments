name: Shodan
slug: shodan
category: agent
description: "IP and host reconnaissance — ports, vulnerabilities, services"
icon: globe
author: NanoSIEM
version: 1
requires_credential: required
credential_fields:
  - name: API_KEY
    label: API Key
    field_type: secret
    required: true
    help: "Get your key at https://account.shodan.io"
allowed_domains:
  - api.shodan.io
config:
  artifact_types: [ip, domain]
  trigger: always
  rate_limit_per_min: 1
  rate_limit_per_day: 100
tags:
  - recon
  - infrastructure
